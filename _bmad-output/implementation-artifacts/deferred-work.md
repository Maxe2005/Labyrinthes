# Deferred Work

## Deferred from: code review of spec-1-8-home-breadcrumb-navigation-settings-access (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `TopBar` draws a full-perimeter 1px border (`highlightthickness=1` on all four sides) instead of the locked mockups' `.topbar { border-bottom: 1px solid var(--c-border) }` (bottom-only).
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/top_bar.py` against `key-builder-edit.html`/`key-player-selection.html`'s `.topbar` CSS rule. Cosmetic only (no AC references border placement); fixing it needs a small restructure (a dedicated bottom-border strip, since Tk's `highlightthickness` has no single-side variant), not a one-line token swap like the other Story 1.8 styling patches, so it was left for a dedicated visual-polish pass.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `Router.navigate()` destroys the previously-mounted screen's frame, which cascades (via Tk's own parent-child `Toplevel` semantics) to silently close any `SettingsWindow` still open on that screen — e.g. opening Settings on Builder, then clicking the breadcrumb's "Home" segment, discards the Settings dialog with no warning.
  evidence: confirmed empirically (destroying a parent `Frame` destroys its child `Toplevel`) and by direct inspection of `src/labyrinthes/app/router.py`'s `navigate()`. Not addressed this story: `SettingsWindow` holds no real, stateful controls yet (Appearance is a placeholder), so there is nothing to lose today; revisit once a later story gives Settings actual persisted state, where silently discarding an open dialog would be more consequential. No AC requires Settings to survive a navigation away from its parent screen.
  Resolved by Story 1.11: `SettingsWindow` is now constructed as `SettingsWindow(parent, theme=theme)` (the app's persistent container) instead of `SettingsWindow(frame, theme=theme)` (the screen's own frame) in each screen's `open_settings()`, so it is no longer a Tk child of the frame `Router.navigate()` destroys -- it genuinely survives navigation now, across Home/Builder/Player, pinned by regression tests in each screen's test file plus one end-to-end test in `tests/app/test_composition_root.py`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: The `Breadcrumb`'s clickable "Home" segment (the sole way back to Home from Builder/Player) is only reachable via a raw `<Button-1>` bind on a `tk.Label`, with no keyboard focus/activation path (no `takefocus`, no `<Return>`/`<space>` binding) — mouse-only.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/breadcrumb.py`. Matches the same, already-existing gap in every other clickable `common/` widget built so far (`IconButton`, `PillButton`, `ToolButton` — none is keyboard-focusable either), which is explicitly Story 1.10's stated scope ("Accessibility floor & keyboard shortcut consistency"), not this story's or Story 1.6's.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `Theme.LIGHT` is hardcoded independently in five separate call sites (`home/screen.py`, `builder/screen.py`, `player/screen.py`'s `mount()`s, plus each of their `SettingsWindow`-opening closures) with no single shared source of truth.
  evidence: confirmed by direct inspection — each is a local `theme = Theme.LIGHT` literal. Explicitly, deliberately scoped this way by this story's spec ("every screen hardcodes `Theme.LIGHT` for now — Story 1.9 wires real theme selection end-to-end"); flagged here so Story 1.9's implementer knows to consolidate all five call sites, not just the three `mount()` ones.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `TopBar.__init__`'s `if breadcrumb_segments is not None:` check doesn't distinguish `None` from `[]` — an empty list still constructs a (visually empty) `Breadcrumb` instance, so `top_bar._breadcrumb` ends up non-`None` even though nothing renders, diverging from the "no breadcrumb at depth 0" contract other code (and tests) check via `is None`.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/top_bar.py:58`. Not reachable today: every current call site passes either `None` (Home) or a concrete 2-element list (Builder/Player), never `[]`.

## Deferred from: code review of 1-1-domain-model-foundation (2026-08-05)

- `Grid.filled` has no type validation; a non-`int` width/height raises a raw `TypeError` instead of `DomainValidationError` [src/labyrinthes/domain/grid.py:54] — deferred, pre-existing pattern across the whole diff (no value object in this story validates argument *types*, only business-domain invariants; consistent with the codebase having no static type-checker configured yet).
- `MazeId` performs zero validation — an empty/whitespace string is accepted as a valid opaque id [src/labyrinthes/domain/maze_id.py:11] — deferred, the story explicitly scopes `MazeId` to "an opaque identifier only, no minting scheme here"; a validation rule is better decided alongside Story 1.4's minting function.

## Deferred from: code review of spec-1-2-automated-domain-ui-boundary-test (2026-08-06)

- `TYPE_CHECKING`-guarded imports are not excluded from the AST scan and would false-positive once `domain/`/`application/` first use type-only imports [tests/test_architecture_boundaries.py] — deferred, pre-existing scope boundary; revisit when a story first needs a `TYPE_CHECKING` import in `domain/`/`application/`.
- Minor untested/low-probability edge branches in the scanner: bare `from . import x` (logic re-read, correct), a relative import climbing above the top-level package (Python itself would crash on this at runtime), a directory shadowed by a same-named file [tests/test_architecture_boundaries.py] — deferred, no reachable consequence today.
- `_format_violations` reports no line number, only file + module [tests/test_architecture_boundaries.py:117] — deferred, optional debuggability improvement, not required by any AC.
- A future `adapters/tkinter/__init__.py` barrel re-export would bypass the screen-isolation check [tests/test_architecture_boundaries.py] — deferred, speculative/forward-looking, no such file exists yet.

## Deferred from: code review of spec-1-4-concrete-mazerepository-single-shared-csv-read-write-implementation (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-concrete-mazerepository-single-shared-csv-read-write-implementation.md`
  summary: `write_maze_csv` (`src/labyrinthes/adapters/storage/csv_maze_format.py`) truncates and writes in place, not atomically (no temp-file-plus-rename) — a crash or interruption mid-write can leave a corrupted or empty maze file, destroying whatever was previously saved under that name.
  evidence: confirmed by direct inspection — `write_maze_csv` opens `path` in `"w"` mode (immediate truncation) and writes rows incrementally via `csv.writer`; there is no atomic-replace fallback. Mirrors the legacy app's own non-atomic `save_as`/`save_param_defaut` writes, so it is not a regression, but the rewrite has no NFR yet committing to fixing it either.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-concrete-mazerepository-single-shared-csv-read-write-implementation.md`
  summary: `read_maze_csv` (`src/labyrinthes/adapters/storage/csv_maze_format.py`) raises raw `IndexError`/`ValueError` on a truncated, too-short, or non-numeric-header maze CSV instead of a typed `LabyrinthesError` subclass.
  evidence: confirmed by direct inspection — `lines[0].split(",")`/`lines[1].split(",")`/`remaining[0]` index unconditionally with no length check, and `int(value)` has no `try`/`except`. Out of this story's declared minimal/mechanical scope (mirrors Story 1.3's `RecordsRepository`-style restraint); best paired with a future format-validation or Epic 4 migration-hardening pass, where corrupt/legacy-edge-case files are already a live concern.

## Deferred from: code review of spec-1-5-concrete-settingsrepository-scoped-persistence (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-concrete-settingsrepository-scoped-persistence.md`
  summary: `write_setting_value` (`src/labyrinthes/adapters/storage/settings_format.py`) truncates and writes in place, not atomically (no temp-file-plus-rename) — a crash mid-write, or a concurrent `get()` from another `JsonSettingsRepository` instance sharing the same root (the exact Builder/Game-running-at-once scenario this story targets), can observe a corrupted or empty setting file.
  evidence: confirmed by direct inspection — `write_setting_value` opens `path` in `"w"` mode (immediate truncation) and writes via `json.dump` with no atomic-replace fallback. Mirrors `write_maze_csv`'s (Story 1.4) identical, already-deferred gap — not a new regression, but the rewrite has no NFR yet committing to fixing it for either writer.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-concrete-settingsrepository-scoped-persistence.md`
  summary: `JsonSettingsRepository.get()`/`read_setting_value` raise raw `json.JSONDecodeError`/`FileNotFoundError` rather than a typed `LabyrinthesError` subclass when a setting file is malformed, corrupted, or removed/replaced between the `path.is_file()` check and the read (a TOCTOU gap).
  evidence: confirmed by direct inspection — `read_setting_value` calls `json.load(handle)` with no `try`/`except`, and `JsonSettingsRepository.get()`'s `is_file()` check and the subsequent read are two separate, non-atomic steps. Mirrors `read_maze_csv`'s (Story 1.4) identical, already-deferred finding for malformed CSV content; out of this story's declared minimal/mechanical scope, best paired with a future format-validation or hardening pass.

## Deferred from: code review of spec-1-6-design-token-system-shared-tkinter-widget-primitives (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `FontSpec.to_tk_font()` (`src/labyrinthes/adapters/tkinter/common/tokens.py`) calls `int(self.weight)` unconditionally, so a `FontSpec` built with a non-numeric `weight` string raises `ValueError` instead of degrading to a valid Tk `"normal"`/`"bold"` weight.
  evidence: confirmed by direct inspection — no `try`/`except` around the `int()` call. Not reachable today: all 7 `TYPOGRAPHY` entries this story defines use numeric-string weights (`"400"`/`"600"`/`"700"`); becomes live risk only if a future caller constructs its own `FontSpec` with a non-numeric weight.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `SPACING`/`RADII` (`src/labyrinthes/adapters/tkinter/common/tokens.py`) are plain mutable module-level `dict`s with no `MappingProxyType`/frozen guard, so one importer mutating a key (e.g. `SPACING["xs"] = 999`) silently changes spacing/radii for every other module sharing the same imported dict.
  evidence: confirmed by direct inspection — both are declared as ordinary `dict[str, ...]` literals, unlike `ColorTokens`/`FontSpec`, which are frozen dataclasses. No AC in this story commits to immutability for these two token categories.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `ToolButtonGroup.activate()` (`src/labyrinthes/adapters/tkinter/common/tool_btn.py`) called with a `ToolButton` that was never registered in that group silently deactivates every member instead of raising, leaving the group with zero active members and no error surfaced.
  evidence: confirmed by direct inspection — the loop iterates `self._buttons` and compares each `candidate is button` with no membership check on `button` itself. Only reachable via manual group/button wiring that bypasses `ToolButton.__init__`'s own `group.add(self)` auto-registration, which is the only construction path this story uses/tests.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: a grouped `ToolButton` that is `.destroy()`ed while still registered in its `ToolButtonGroup` (no `remove()`/unregister API exists) raises `_tkinter.TclError: bad window path name` out of the group's next `activate()` call, breaking every subsequent tool selection in that group, not just the destroyed button.
  evidence: confirmed by direct inspection — `activate()` calls `candidate._set_active_direct()` → `_apply_style()` → `self.configure(...)` on every registered member unconditionally, including a destroyed one. Not reachable today: no consuming screen exists yet (Stories 1.7+) to ever call `.destroy()` on a `ToolButton`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `Tooltip` (`src/labyrinthes/adapters/tkinter/common/tooltip.py`) only binds `<Enter>`/`<Leave>` on its anchor widget, with no `<Destroy>` handling -- if the anchor is destroyed while the popup is shown, without a preceding `<Leave>`, the popup `Toplevel` can be left referenced with no owning widget to trigger its normal hover-leave cleanup.
  evidence: confirmed by direct inspection — `_on_enter`/`_on_leave` are the only two bindings registered in `__init__`. Affects every primitive that attaches a `Tooltip` (`ToolButton`, `IconButton`); not reachable today since no consuming screen performs teardown yet.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `HudChip(value=None)`/`HudChip.set_value(None)` (`src/labyrinthes/adapters/tkinter/common/hud_chip.py`) renders the literal text `"None"` instead of a placeholder, since `value: object` type-checks fine for `None` and `str(value)` is applied unconditionally.
  evidence: confirmed by direct inspection — no `None`/empty special-case before `str(value)`. Reachable only if a future caller passes an unset stat straight through rather than a placeholder string itself; no current call site in this story does so (no consuming screen exists yet).

## Deferred from: code review of spec-1-7-single-composition-root-screen-router (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()` (`src/labyrinthes/app/router.py`) has no exception-safety if a registered `mount()` callable raises mid-construction — any widgets it already created are orphaned (never tracked, never destroyed) and the exception propagates raw, with no test exercising "a screen's own `mount()` blows up."
  evidence: confirmed by direct inspection — `navigate()` calls `mount(self._container, state)` with no `try`/`except` around it. Not reachable today: all three registered screens are static placeholders that never raise.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()` never validates that the value returned by a registered `mount()` is actually a `tk.Frame` before calling `.pack()` on it — a misbehaving screen implementation surfaces as a bare `AttributeError` instead of a clear, typed error.
  evidence: confirmed by direct inspection — `new_frame = mount(self._container, state)` is used directly with no `isinstance` check. Not reachable today: all three placeholder `mount()` implementations correctly return a `tk.Frame`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()` is not reentrancy-safe — a `mount()` callback that itself calls `router.navigate()` (e.g. a future screen redirecting during its own construction) would interleave two in-flight updates to `_current_frame`/`_current_screen_id`, corrupting the router's bookkeeping.
  evidence: confirmed by direct inspection — `_current_frame`/`_current_screen_id` are plain instance attributes mutated at the end of `navigate()`, with no reentrancy guard. Not reachable today: no consuming screen calls `navigate()` from within its own `mount()`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()`'s `previous_frame.destroy()` call is unguarded — if it raises (e.g. the frame's underlying Tk window is already gone), the following `current_screen_id`/`_current_frame` bookkeeping update never runs, leaving the router's internal state pointing at the just-torn-down screen while the new one is already visible and packed.
  evidence: confirmed by direct inspection — no `try`/`except` around `previous_frame.destroy()`, and the state-update lines execute strictly after it. Not reachable today: nothing external ever destroys a router-owned frame out of band.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.register()` (`src/labyrinthes/app/router.py`) silently overwrites an existing `ScreenId` registration, and `Router.navigate()` always tears down and rebuilds the current screen even when re-navigating to the screen already mounted — neither behavior is tested or documented as intentional.
  evidence: confirmed by direct inspection — `register()` is a plain dict assignment (`self._mounts[screen_id] = mount`), and `navigate()` has no early-return guard comparing `screen_id` to `self._current_screen_id`. Not reachable today: `composition_root.build_app()` registers each `ScreenId` exactly once and no screen re-navigates to itself.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-single-composition-root-screen-router.md`
  summary: `tests/app/test_router.py`'s unregistered-screen test only verifies the previous screen's frame still exists (`winfo_exists()`) after a failed `navigate()`, not that it is still visually mapped (`winfo_ismapped()`) — a regression that merely unpacked (without destroying) the current frame on a failed navigate would pass this suite undetected.
  evidence: confirmed empirically that the seemingly-obvious fix doesn't directly apply — `winfo_ismapped()` returns `False` for any packed widget under this suite's own convention of an immediately-`withdraw()`-ed `tk_root`, so asserting it `True` would false-fail even on correct behavior; a real fix needs a different verification approach (e.g. checking the widget is still a child of the container / still gridded-or-packed via `pack_info()`), left open rather than patched blind.

## Deferred from: code review of spec-1-9-light-dark-theme-toggle-wired-end-to-end (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `ThemeController(settings_repository)` is now constructed unconditionally on every real app launch (`composition_root.build_app()`), which is the first real, unconditional call site to actually reach `SettingsRepository.get()`/`JsonSettingsRepository.get()` on startup — escalating the already-deferred Story 1.5 finding that `read_setting_value` raises a raw `json.JSONDecodeError` on a malformed/truncated settings file from a hypothetical, "not reachable today" risk into a guaranteed app-launch crash the first time a corrupted `settings/shared/theme.json` exists (e.g. from the also-already-deferred Story 1.5 non-atomic-write finding leaving a half-written file after a crash).
  evidence: confirmed by direct inspection — `ThemeController._load_theme()` (this story) now catches `SettingNotFoundError`/`ValueError` but not `json.JSONDecodeError`, and `JsonSettingsRepository.get()`/`read_setting_value` still have no `try`/`except` around `json.load()` (unchanged since Story 1.5). Not patched here: fixing it means either `JsonSettingsRepository` raising a typed `LabyrinthesError` subclass on malformed JSON (a cross-cutting, repository-level fix already flagged in Story 1.5's own deferred entry as needing "a future format-validation or hardening pass") or every individual consumer defensively catching `json.JSONDecodeError`, which would leak a storage-adapter-specific exception type into `application`-layer callers. `ThemeController` was patched during this story's review to at least catch `ValueError` (an unrecognized-but-well-formed stored value) and default to `Theme.LIGHT`, closing the narrower half of this gap.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `ThemeController.toggle()` has no error handling around `self._settings.set(...)` — a persistence failure (disk full, read-only filesystem, permissions) propagates raw out of the theme-toggle `IconButton`'s Tk click callback, where Tkinter's default `report_callback_exception` only dumps a traceback to stderr; the user sees no feedback and the theme silently fails to change (though the current, in-memory `Theme` still flips and is applied to the re-rendered screen, so the visible UI and the failed-to-persist setting diverge until next restart).
  evidence: confirmed by direct inspection — `toggle()` calls `self._settings.set(...)` with no surrounding `try`/`except`. Same class as the already-deferred Story 1.4/1.5 "no error handling around repository writes" findings; not novel to this story, just reached by a new call site.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `build_app()`'s default `JsonSettingsRepository()` root (`DEFAULT_SETTINGS_ROOT = Path("settings")`, Story 1.5) is relative to the process's current working directory, with no XDG/home-directory anchoring — this story is the first to actually activate that default from a live, unconditional code path (theme persistence), so launching the real app from two different working directories now visibly and silently splits/duplicates the persisted theme (and will do the same for every other future `shared`-scope setting).
  evidence: confirmed by direct inspection — `settings_paths.py`'s `DEFAULT_SETTINGS_ROOT` is unchanged since Story 1.5; `composition_root.build_app()`'s new `settings_repository` parameter defaults to `JsonSettingsRepository()` with no root override. Pre-existing default, newly load-bearing.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `ThemeController.toggle()` iterates `self._listeners` with no isolation — if one subscriber's callback raises, every listener registered after it in `subscribe()` order never runs, even though the theme was already flipped and persisted before the loop started, leaving the persisted/in-memory `Theme` and the set of screens that actually re-rendered out of sync.
  evidence: confirmed by direct inspection — `toggle()`'s `for listener in self._listeners: listener(new_theme)` has no per-listener `try`/`except`. Not reachable today: exactly one listener (`composition_root.build_app()`'s `on_theme_change`) is ever registered.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `composition_root.build_app()`'s `navigate()` closure sets `last_state` before calling `router.navigate(screen_id, state)`, not after it succeeds — if a future screen's `mount()` raises mid-construction, `last_state` is already updated to the new (never-actually-mounted) state while `router.current_screen_id` still points at the old, unchanged screen, so a subsequent theme toggle would re-navigate the old screen with the wrong state.
  evidence: confirmed by direct inspection of the `navigate()` closure's statement order. Same root cause as the already-deferred Story 1.7 finding "`Router.navigate()` has no exception-safety if a registered `mount()` callable raises mid-construction" — not reachable today for the identical reason (all three registered screens are static placeholders that never raise).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: A theme-triggered re-navigate that raises (via `on_theme_change` → `navigate()` → `router.navigate()`, itself reachable only if a future screen's `mount()` raises) is not caught anywhere — `build_app()`'s own `try`/`except Exception: root.destroy(); raise` only wraps the initial construction phase, not later event-handler-triggered calls — so `ThemeController`'s in-memory/persisted theme would already reflect the new value while `Router` keeps the old frame mounted, and the exception would propagate uncaught out of the theme-toggle `IconButton`'s Tk click callback.
  evidence: confirmed by direct inspection — `on_theme_change`'s `navigate(current_screen_id, last_state)` call has no surrounding `try`/`except`, and `build_app()`'s own exception guard is scoped to the function body, not to callbacks invoked later from the Tk event loop. Same root cause and same "not reachable today" precondition as the two findings above and the pre-existing Story 1.7 entries on `Router.navigate()`'s lack of exception-safety.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: No test exercises the `on_theme_change` guard for "theme toggled before any `navigate()`" (`if current_screen_id is not None`) that this story's own spec I/O matrix calls out as an edge case — the guard is correct by inspection but not directly reachable through `build_app()`'s public surface (which always navigates to Home before returning), so a regression that deleted it would go undetected until an actual future entry point made it reachable.
  evidence: confirmed by direct inspection — `on_theme_change` is a local closure inside `build_app()`, not independently exposed for unit testing, and no existing test constructs a `ThemeController`/router pair in the pre-first-navigate state to exercise this branch.

## Deferred from: spec-1-10-accessibility-floor-keyboard-shortcut-consistency planning (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `settings_window.py`'s own docstring anticipates a "Shortcuts" category alongside Appearance/Ball/Difficulty, but no AC in Story 1.10 requires building real category-switching UI in `SettingsWindow`, so it was not added here.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/settings_window.py`'s module docstring ("Ball/Difficulty/Shortcuts don't exist as domain concepts yet (Epics 2/3, Story 1.10)"). This story's own intent contract explicitly forbids adding a "Shortcuts" category to `SettingsWindow` as unrequested scope; flagged here for whichever future story next adds real `SettingsWindow` content (e.g. Story 2.11 Appearance).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: The dark-mode active-`ToolButton` text/background pairing (`colors.accent` `#3b82f6` on `colors.accent_bg` dark `#16233d`) measures ~4.25:1, marginally under the 4.5:1 AA text threshold, even though `DESIGN.md` explicitly asserts this pairing "was not flagged" (i.e. assessed as fine during the design phase).
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/tool_btn.py`'s `_apply_style()` (dark-mode active branch: `text_color = colors.accent`) against `DESIGN.md`'s locked color pairs. Not fixed here since it would mean introducing a third AA-fix token beyond the two `DESIGN.md` explicitly locks (`accent-on-tint`, `accent-strong-dark`), overriding a locked planning artifact's own stated decision unilaterally — this story's intent contract explicitly forbids touching it. Also not reachable today: no consuming screen renders an active `ToolButton` in production yet (only tests construct one directly).

## Deferred from: code review of spec-1-10-accessibility-floor-keyboard-shortcut-consistency (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: Pressing "B"/"P" (Home's canonical shortcuts) while a non-modal `SettingsWindow` is open silently closes it -- `bind_shortcut()` fires regardless of which widget has focus, triggering `Router.navigate()`, which destroys Home's `frame`; since `SettingsWindow` is a real Tk child `Toplevel` of that `frame` (`home/screen.py`'s `open_settings()`), it gets destroyed as a cascade side-effect.
  evidence: confirmed empirically (`frame.destroy()` after constructing a real `SettingsWindow(frame, ...)` leaves `settings.winfo_exists() == 0`), and by direct inspection of `home/screen.py`/`keybindings.py`. Not fixed here: this is the identical underlying mechanism -- `Router.navigate()`'s frame-destroy cascading into an open child `SettingsWindow` -- already identified and explicitly deferred twice before (Story 1.8's breadcrumb-click finding, Story 1.9's theme-toggle finding), both accepted with "`SettingsWindow` holds no real, stateful controls yet... no AC requires Settings to survive a navigation away." This story only adds a third trigger (a keyboard shortcut) for the same already-accepted tradeoff, not a new failure mode; revisit alongside the other two once `SettingsWindow` gains real persisted state worth protecting.
  Resolved by Story 1.11: `SettingsWindow` is now constructed as `SettingsWindow(parent, theme=theme)` (the app's persistent container) instead of `SettingsWindow(frame, theme=theme)` (the screen's own frame) in each screen's `open_settings()`, so it is no longer a Tk child of the frame `Router.navigate()` destroys -- it genuinely survives navigation now, across Home/Builder/Player, pinned by regression tests in each screen's test file plus one end-to-end test in `tests/app/test_composition_root.py`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `bind_shortcut()` registers its handler via `widget.bind_all()` with no check on `event.widget`'s class, so a future screen that adds a text `Entry`/`Spinbox` whose typed content includes "b" or "p" would have those keystrokes hijacked into navigation instead of reaching the text field.
  evidence: confirmed by direct inspection of `keybindings.py`'s `bind_shortcut()` -- no widget-class guard exists. Not reachable today: no screen anywhere in the app has a text input yet (Home/Builder/Player are all still non-text-entry placeholders or button-only). Whichever future story first adds a text field to a screen that also registers a canonical shortcut needs to add this guard (e.g. skip firing when `event.widget` is an `Entry`/`Spinbox`/`Text`).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `IconButton` snapshots `self._colors = colors_for(theme)` once at construction and reuses that snapshot for its focus-ring styling, while the three sibling widgets touched by this same story (`ToolButton`, `PillButton`, `Breadcrumb`) all recompute `colors_for(self._theme)` fresh inside their restyle methods -- a stylistic inconsistency introduced across four otherwise-parallel implementations of the identical focus-ring feature.
  evidence: confirmed by direct inspection of `icon_btn.py` vs `tool_btn.py`/`pill_btn.py`/`breadcrumb.py`. Harmless today: every screen fully re-mounts (a fresh widget instance, hence a fresh snapshot) on every theme toggle (Story 1.9's `Router.navigate()`-based re-render), so a stale cached `Theme` is never actually observable in practice. Worth aligning `IconButton` to the same recompute-fresh pattern as its siblings in a future cleanup pass, not urgent.

## Deferred from: follow-up code review of spec-1-10-accessibility-floor-keyboard-shortcut-consistency (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: Toggling `highlightthickness` between `RESTING_RING_THICKNESS` (1) and `FOCUS_RING_THICKNESS` (2) on focus changes the outer footprint of `PillButton`/`ToolButton`/`Breadcrumb` segments -- any focusable `common/` widget without a fixed size -- causing a ~2px layout shift in surrounding siblings each time focus enters or leaves; `IconButton` is unaffected only because its fixed `width`/`height` plus `pack_propagate(False)` absorb the change.
  evidence: confirmed empirically -- a naturally-sized `tk.Frame`'s reported width grows from 98px to 100px when `highlightthickness` is bumped 1->2 with no other geometry constraint, and `PillButton`/`ToolButton` both use plain `tk.Frame` with no fixed width/height. Not fixed here: avoiding the shift needs a different mechanic than "thicker highlight ring" (e.g. always reserving 2px of highlight space and toggling only its color), which the Design Notes explicitly ruled out for `ToolButton` -- color alone can't distinguish active-unfocused from active-focused there, per the module's own documented rationale. A genuine redesign trade-off, not a mechanical fix, and no AC requires layout stability under focus.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `bind_shortcut()`'s `_current_registration` registry keys registrations on `id(widget.tk)` as a stand-in for "which Tk interpreter", a CPython object-identity shortcut that a later, unrelated `tk.Tk()` instance could reuse once an earlier one is garbage-collected.
  evidence: confirmed by direct inspection -- `interpreter_id = id(widget.tk)` (`keybindings.py`) has no stronger identity guarantee than CPython's `id()` semantics. Not reachable today in the real app (exactly one `Tk()` root lives for the whole process) or in the current test suite (each `tk_root`/child-widget fixture's `<Destroy>` cleanup removes its registry entries synchronously before the next `tk.Tk()` is constructed), but a future fixture that constructs/destroys `Tk()` instances without waiting for teardown would need a sturdier identity than `id()`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `bind_shortcut()`'s `.lower()`/`.upper()`-based sequence construction (and `Keybinding.event`/`.display`) silently assumes every `key` is a single ASCII letter; a future `Keybinding` using a function-key name (e.g. `"F1"`) would produce a syntactically valid but semantically wrong Tk sequence (`<KeyPress-f1>` is not a real X11 keysym) that would simply never fire, with no error raised anywhere.
  evidence: confirmed by direct inspection -- `bind_shortcut()` and `Keybinding.display`/`.event` all treat `key` as case-invertible with no validation. Not reachable today: `KEYBINDINGS` only holds `"b"`/`"p"`, both single ASCII letters. Worth a guard (e.g. `len(key) == 1 and key.isalpha()`) or a documented constraint whenever a future story adds a non-letter shortcut.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `IconButton`/`ToolButton`/`PillButton` all now set `takefocus=True` unconditionally in `__init__`, even when constructed with `command=None`, creating a keyboard tab-stop that does nothing on Enter/Space.
  evidence: confirmed by direct inspection of all three widgets' `__init__` plus their shared `_on_click()` no-op-when-`command`-is-`None` pattern. Not reachable today: every current call site (`top_bar.py`'s two `IconButton`s, `home/screen.py`'s two `PillButton`s) passes a real `command`; would only surface once a future screen constructs one of these widgets purely decoratively.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `IconButton`/`ToolButton`/`PillButton`/`Breadcrumb`'s new `<Return>`/`<space>` bindings fire on every `KeyPress`, so holding the key down triggers the OS's key-auto-repeat and re-invokes the command/navigation/`ToolButtonGroup.activate()` repeatedly for as long as it's held -- a mouse click can't do this, since `<Button-1>` only fires once per physical press.
  evidence: confirmed by direct inspection -- all four widgets bind their activation handler to `<Return>`/`<space>` (`KeyPress`), not `<KeyRelease>` or any auto-repeat guard. Low impact for the current idempotent handlers (`Router.navigate()` to the same screen, `ToolButtonGroup.activate()` on an already-active button), but worth a debounce (e.g. binding `<KeyRelease>` instead, or tracking key-down state) before a future non-idempotent command is wired to one of these widgets.

## Deferred from: code review of 1-11-settings-dialog-survives-screen-navigation (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/1-11-settings-dialog-survives-screen-navigation.md`
  summary: `open_settings()` has no guard against opening multiple `SettingsWindow` instances -- repeated settings-icon clicks stack independent `Toplevel`s with no dedup, across all three screens (`home/screen.py`, `builder/screen.py`, `player/screen.py`).
  evidence: confirmed by direct inspection -- each `open_settings()` closure unconditionally calls `SettingsWindow(...)` with no check for an already-open instance. Not fixed here: the construction call's target widget changed (`parent` instead of `frame`, this same story's real fix), but the missing dedup guard predates this diff and is out of its scope. Revisit if/when `SettingsWindow` gains real state where stacked duplicate dialogs would be confusing or lossy -- worth prioritizing a little sooner than before, now that `SettingsWindow` survives navigation and so can accumulate across more of them than a closing one ever could.

## Deferred from: code review of 1-12-persistence-hardening-atomic-writes-typed-errors (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/1-12-persistence-hardening-atomic-writes-typed-errors.md`
  summary: `read_maze_csv`'s `path.read_text(encoding="utf-8")` call and `read_setting_value`'s `json.load(handle)` call can each raise a raw `UnicodeDecodeError` on invalid-UTF-8/binary content -- not a `LabyrinthesError` subclass, so it isn't caught by either function's new typed-error wrapping (`MazeCorruptError`/`SettingCorruptError`).
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/storage/csv_maze_format.py:44` (the `read_text` call sits before the `try` block that wraps only header parsing) and `src/labyrinthes/adapters/storage/settings_format.py:28-32` (`json.load` reads the handle internally, so a decode error surfaces from inside the `try` but isn't a `json.JSONDecodeError` subclass, so the `except` clause doesn't catch it). Pre-existing gap -- this code path had zero error handling before this story's diff -- and narrower than Task 2/Task 3's explicitly stated scope (header-parsing / `json.JSONDecodeError` only), so not a regression introduced here. Worth widening the guard (or adding `UnicodeDecodeError` to each except tuple) whenever a story next touches either reader, since `MazeCorruptError`'s own docstring already claims the broader "content can't be parsed into a Maze" intent.

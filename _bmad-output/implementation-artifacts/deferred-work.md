# Deferred Work

## Deferred from: code review of spec-2-6-levels-progressive-visibility-1-4-max (2026-08-13)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-6-levels-progressive-visibility-1-4-max.md`
  summary: Level-4 reveal threshold is degenerate at real maze sizes — `round(total_interior_walls/(d+1))` at D1 requires ~half of a 10×10 grid's ~180 interior walls to be discovered before AC-4's "all discovered walls hide again" fires, so walls effectively accumulate until solve. Spec-pinned placeholder formula; explicitly the Story-2.7 seam (which also wires the Difficulty control that parameterizes it).
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-6-levels-progressive-visibility-1-4-max.md`
  summary: `reveal_threshold` uses Python's banker's `round()` (`round(0.5)==0`, `round(2.5)==2`) whereas the legacy `arrondi` was half-up; for a maze with 1 interior wall at D1 the threshold is 0, so every discovery immediately resets to just that wall (equivalent rendering, but diverges from legacy semantics at fractional boundaries). Spec-pinned `round(...)`; Story-2.7 finalizes the function.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-6-levels-progressive-visibility-1-4-max.md`
  summary: `MazeCanvas.redraw_structure` deletes and recreates `"wall"`/`"contour"` items, which stack them above the constructor-drawn entry/exit markers and ball in canvas z-order (constructor order was walls → markers → ball). No geometric overlap today (ball radius is a fraction of a cell and walls sit on cell borders), so visually benign; latent if wall thickness or marker sizing ever changes.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-6-levels-progressive-visibility-1-4-max.md`
  summary: Wall-segment decoding is duplicated across four independent walkers — `level_visibility._all_walls`, `total_interior_walls`, `visible_walls`, and `MazeCanvas._draw_walls` — each walking the raw grid with subtly different semantics (full grid vs partition-boundary clipping). Drift risk; a single "walls of this grid" primitive would remove it.

## Deferred from: code review of spec-2-5-movement-modes-smooth-vs-discrete-configurable-speed (2026-08-13)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-5-movement-modes-smooth-vs-discrete-configurable-speed.md`
  summary: The "Movement" sidebar group is built as a plain `Frame` with two `ToolButton`s plus manual active-state sync, not a `ToolButtonGroup` as the Code Map specifies. Functionally fine (only a single boolean toggle exists, so group exclusivity adds nothing); revisit when the sidebar gains more toggle groups.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-5-movement-modes-smooth-vs-discrete-configurable-speed.md`
  summary: Post-solve, the sidebar `_toggle_mode`/`_cycle_speed` handlers still run: they call the session no-op (`set_mode`/`set_speed` return unchanged once solved), then write the new value to settings and update the button label/active state, so the button can disagree with the frozen session. Cosmetic on a finished run; persisting the preference for the next session is arguably desirable.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-5-movement-modes-smooth-vs-discrete-configurable-speed.md`
  summary: `movement_settings._read_member` catches only `SettingNotFoundError`/`SettingCorruptError`/`ValueError`/`TypeError`; any other exception from `settings.get` would propagate despite the "Never raises" docstring. Parity with the existing `maze_size_bounds._read_bound` pattern; revisit if a concrete repository can raise other error types.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-5-movement-modes-smooth-vs-discrete-configurable-speed.md`
  summary: `_SPEED_CYCLE = tuple(MovementSpeed)` depends on enum member declaration order for the Slow→Normal→Fast cycle; a future reorder would silently change the cycle. Enum order is stable today.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-5-movement-modes-smooth-vs-discrete-configurable-speed.md`
  summary: No screen-level test that a movement key after solving is a no-op (doesn't reschedule an animation job). Domain solved-guard tests cover the underlying behavior.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-5-movement-modes-smooth-vs-discrete-configurable-speed.md`
  summary: No corrupt-settings-at-mount integration test for the player screen. The never-raise guarantee is covered by `test_movement_settings` unit tests.

## Deferred from: code review of spec-1-8-home-breadcrumb-navigation-settings-access (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `TopBar` draws a full-perimeter 1px border (`highlightthickness=1` on all four sides) instead of the locked mockups' `.topbar { border-bottom: 1px solid var(--c-border) }` (bottom-only).
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/top_bar.py` against `key-builder-edit.html`/`key-player-selection.html`'s `.topbar` CSS rule. Cosmetic only (no AC references border placement); fixing it needs a small restructure (a dedicated bottom-border strip, since Tk's `highlightthickness` has no single-side variant), not a one-line token swap like the other Story 1.8 styling patches, so it was left for a dedicated visual-polish pass.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `Router.navigate()` destroys the previously-mounted screen's frame, which cascades (via Tk's own parent-child `Toplevel` semantics) to silently close any `SettingsWindow` still open on that screen — e.g. opening Settings on Builder, then clicking the breadcrumb's "Home" segment, discards the Settings dialog with no warning.
  evidence: confirmed empirically (destroying a parent `Frame` destroys its child `Toplevel`) and by direct inspection of `src/labyrinthes/app/router.py`'s `navigate()`. Not addressed this story: `SettingsWindow` holds no real, stateful controls yet (Appearance is a placeholder), so there is nothing to lose today; revisit once a later story gives Settings actual persisted state, where silently discarding an open dialog would be more consequential. No AC requires Settings to survive a navigation away from its parent screen.
  Resolved by Story 1.11: `SettingsWindow` is now constructed as `SettingsWindow(parent, theme=theme)` (the app's persistent container) instead of `SettingsWindow(frame, theme=theme)` (the screen's own frame) in each screen's `open_settings()`, so it is no longer a Tk child of the frame `Router.navigate()` destroys -- it genuinely survives navigation now, across Home/Builder/Player, pinned by regression tests in each screen's test file plus one end-to-end test in `tests/app/test_composition_root.py`.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: The `Breadcrumb`'s clickable "Home" segment (the sole way back to Home from Builder/Player) is only reachable via a raw `<Button-1>` bind on a `tk.Label`, with no keyboard focus/activation path (no `takefocus`, no `<Return>`/`<space>` binding) — mouse-only.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/breadcrumb.py`. Matches the same, already-existing gap in every other clickable `common/` widget built so far (`IconButton`, `PillButton`, `ToolButton` — none is keyboard-focusable either), which is explicitly Story 1.10's stated scope ("Accessibility floor & keyboard shortcut consistency"), not this story's or Story 1.6's.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `Theme.LIGHT` is hardcoded independently in five separate call sites (`home/screen.py`, `builder/screen.py`, `player/screen.py`'s `mount()`s, plus each of their `SettingsWindow`-opening closures) with no single shared source of truth.
  evidence: confirmed by direct inspection — each is a local `theme = Theme.LIGHT` literal. Explicitly, deliberately scoped this way by this story's spec ("every screen hardcodes `Theme.LIGHT` for now — Story 1.9 wires real theme selection end-to-end"); flagged here so Story 1.9's implementer knows to consolidate all five call sites, not just the three `mount()` ones.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-8-home-breadcrumb-navigation-settings-access.md`
  summary: `TopBar.__init__`'s `if breadcrumb_segments is not None:` check doesn't distinguish `None` from `[]` — an empty list still constructs a (visually empty) `Breadcrumb` instance, so `top_bar._breadcrumb` ends up non-`None` even though nothing renders, diverging from the "no breadcrumb at depth 0" contract other code (and tests) check via `is None`.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/top_bar.py:58`. Not reachable today: every current call site passes either `None` (Home) or a concrete 2-element list (Builder/Player), never `[]`.

## Deferred from: code review of 1-1-domain-model-foundation (2026-08-05)

- `Grid.filled` has no type validation; a non-`int` width/height raises a raw `TypeError` instead of `DomainValidationError` [src/labyrinthes/domain/grid.py:54] — deferred, pre-existing pattern across the whole diff (no value object in this story validates argument *types*, only business-domain invariants; consistent with the codebase having no static type-checker configured yet).
- `MazeId` performs zero validation — an empty/whitespace string is accepted as a valid opaque id [src/labyrinthes/domain/maze_id.py:11] — deferred, the story explicitly scopes `MazeId` to "an opaque identifier only, no minting scheme here"; a validation rule is better decided alongside Story 1.4's minting function.

## Deferred from: code review of spec-1-2-automated-domain-ui-boundary-test (2026-08-06)

- `TYPE_CHECKING`-guarded imports are not excluded from the AST scan and would false-positive once `domain/`/`application/` first use type-only imports [tests/test_architecture_boundaries.py] — deferred, pre-existing scope boundary; revisit when a story first needs a `TYPE_CHECKING` import in `domain/`/`application/`.
- Minor untested/low-probability edge branches in the scanner: bare `from . import x` (logic re-read, correct), a relative import climbing above the top-level package (Python itself would crash on this at runtime), a directory shadowed by a same-named file [tests/test_architecture_boundaries.py] — deferred, no reachable consequence today.
- `_format_violations` reports no line number, only file + module [tests/test_architecture_boundaries.py:117] — deferred, optional debuggability improvement, not required by any AC.
- A future `adapters/tkinter/__init__.py` barrel re-export would bypass the screen-isolation check [tests/test_architecture_boundaries.py] — deferred, speculative/forward-looking, no such file exists yet.

## Deferred from: code review of spec-1-4-concrete-mazerepository-single-shared-csv-read-write-implementation (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-4-concrete-mazerepository-single-shared-csv-read-write-implementation.md`
  summary: `write_maze_csv` (`src/labyrinthes/adapters/storage/csv_maze_format.py`) truncates and writes in place, not atomically (no temp-file-plus-rename) — a crash or interruption mid-write can leave a corrupted or empty maze file, destroying whatever was previously saved under that name.
  evidence: confirmed by direct inspection — `write_maze_csv` opens `path` in `"w"` mode (immediate truncation) and writes rows incrementally via `csv.writer`; there is no atomic-replace fallback. Mirrors the legacy app's own non-atomic `save_as`/`save_param_defaut` writes, so it is not a regression, but the rewrite has no NFR yet committing to fixing it either.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-4-concrete-mazerepository-single-shared-csv-read-write-implementation.md`
  summary: `read_maze_csv` (`src/labyrinthes/adapters/storage/csv_maze_format.py`) raises raw `IndexError`/`ValueError` on a truncated, too-short, or non-numeric-header maze CSV instead of a typed `LabyrinthesError` subclass.
  evidence: confirmed by direct inspection — `lines[0].split(",")`/`lines[1].split(",")`/`remaining[0]` index unconditionally with no length check, and `int(value)` has no `try`/`except`. Out of this story's declared minimal/mechanical scope (mirrors Story 1.3's `RecordsRepository`-style restraint); best paired with a future format-validation or Epic 4 migration-hardening pass, where corrupt/legacy-edge-case files are already a live concern.

## Deferred from: code review of spec-1-5-concrete-settingsrepository-scoped-persistence (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-5-concrete-settingsrepository-scoped-persistence.md`
  summary: `write_setting_value` (`src/labyrinthes/adapters/storage/settings_format.py`) truncates and writes in place, not atomically (no temp-file-plus-rename) — a crash mid-write, or a concurrent `get()` from another `JsonSettingsRepository` instance sharing the same root (the exact Builder/Game-running-at-once scenario this story targets), can observe a corrupted or empty setting file.
  evidence: confirmed by direct inspection — `write_setting_value` opens `path` in `"w"` mode (immediate truncation) and writes via `json.dump` with no atomic-replace fallback. Mirrors `write_maze_csv`'s (Story 1.4) identical, already-deferred gap — not a new regression, but the rewrite has no NFR yet committing to fixing it for either writer.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-5-concrete-settingsrepository-scoped-persistence.md`
  summary: `JsonSettingsRepository.get()`/`read_setting_value` raise raw `json.JSONDecodeError`/`FileNotFoundError` rather than a typed `LabyrinthesError` subclass when a setting file is malformed, corrupted, or removed/replaced between the `path.is_file()` check and the read (a TOCTOU gap).
  evidence: confirmed by direct inspection — `read_setting_value` calls `json.load(handle)` with no `try`/`except`, and `JsonSettingsRepository.get()`'s `is_file()` check and the subsequent read are two separate, non-atomic steps. Mirrors `read_maze_csv`'s (Story 1.4) identical, already-deferred finding for malformed CSV content; out of this story's declared minimal/mechanical scope, best paired with a future format-validation or hardening pass.

## Deferred from: code review of spec-1-6-design-token-system-shared-tkinter-widget-primitives (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `FontSpec.to_tk_font()` (`src/labyrinthes/adapters/tkinter/common/tokens.py`) calls `int(self.weight)` unconditionally, so a `FontSpec` built with a non-numeric `weight` string raises `ValueError` instead of degrading to a valid Tk `"normal"`/`"bold"` weight.
  evidence: confirmed by direct inspection — no `try`/`except` around the `int()` call. Not reachable today: all 7 `TYPOGRAPHY` entries this story defines use numeric-string weights (`"400"`/`"600"`/`"700"`); becomes live risk only if a future caller constructs its own `FontSpec` with a non-numeric weight.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `SPACING`/`RADII` (`src/labyrinthes/adapters/tkinter/common/tokens.py`) are plain mutable module-level `dict`s with no `MappingProxyType`/frozen guard, so one importer mutating a key (e.g. `SPACING["xs"] = 999`) silently changes spacing/radii for every other module sharing the same imported dict.
  evidence: confirmed by direct inspection — both are declared as ordinary `dict[str, ...]` literals, unlike `ColorTokens`/`FontSpec`, which are frozen dataclasses. No AC in this story commits to immutability for these two token categories.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `ToolButtonGroup.activate()` (`src/labyrinthes/adapters/tkinter/common/tool_btn.py`) called with a `ToolButton` that was never registered in that group silently deactivates every member instead of raising, leaving the group with zero active members and no error surfaced.
  evidence: confirmed by direct inspection — the loop iterates `self._buttons` and compares each `candidate is button` with no membership check on `button` itself. Only reachable via manual group/button wiring that bypasses `ToolButton.__init__`'s own `group.add(self)` auto-registration, which is the only construction path this story uses/tests.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: a grouped `ToolButton` that is `.destroy()`ed while still registered in its `ToolButtonGroup` (no `remove()`/unregister API exists) raises `_tkinter.TclError: bad window path name` out of the group's next `activate()` call, breaking every subsequent tool selection in that group, not just the destroyed button.
  evidence: confirmed by direct inspection — `activate()` calls `candidate._set_active_direct()` → `_apply_style()` → `self.configure(...)` on every registered member unconditionally, including a destroyed one. Not reachable today: no consuming screen exists yet (Stories 1.7+) to ever call `.destroy()` on a `ToolButton`.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `Tooltip` (`src/labyrinthes/adapters/tkinter/common/tooltip.py`) only binds `<Enter>`/`<Leave>` on its anchor widget, with no `<Destroy>` handling -- if the anchor is destroyed while the popup is shown, without a preceding `<Leave>`, the popup `Toplevel` can be left referenced with no owning widget to trigger its normal hover-leave cleanup.
  evidence: confirmed by direct inspection — `_on_enter`/`_on_leave` are the only two bindings registered in `__init__`. Affects every primitive that attaches a `Tooltip` (`ToolButton`, `IconButton`); not reachable today since no consuming screen performs teardown yet.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-6-design-token-system-shared-tkinter-widget-primitives.md`
  summary: `HudChip(value=None)`/`HudChip.set_value(None)` (`src/labyrinthes/adapters/tkinter/common/hud_chip.py`) renders the literal text `"None"` instead of a placeholder, since `value: object` type-checks fine for `None` and `str(value)` is applied unconditionally.
  evidence: confirmed by direct inspection — no `None`/empty special-case before `str(value)`. Reachable only if a future caller passes an unset stat straight through rather than a placeholder string itself; no current call site in this story does so (no consuming screen exists yet).

## Deferred from: code review of spec-1-7-single-composition-root-screen-router (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()` (`src/labyrinthes/app/router.py`) has no exception-safety if a registered `mount()` callable raises mid-construction — any widgets it already created are orphaned (never tracked, never destroyed) and the exception propagates raw, with no test exercising "a screen's own `mount()` blows up."
  evidence: confirmed by direct inspection — `navigate()` calls `mount(self._container, state)` with no `try`/`except` around it. Not reachable today: all three registered screens are static placeholders that never raise.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()` never validates that the value returned by a registered `mount()` is actually a `tk.Frame` before calling `.pack()` on it — a misbehaving screen implementation surfaces as a bare `AttributeError` instead of a clear, typed error.
  evidence: confirmed by direct inspection — `new_frame = mount(self._container, state)` is used directly with no `isinstance` check. Not reachable today: all three placeholder `mount()` implementations correctly return a `tk.Frame`.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()` is not reentrancy-safe — a `mount()` callback that itself calls `router.navigate()` (e.g. a future screen redirecting during its own construction) would interleave two in-flight updates to `_current_frame`/`_current_screen_id`, corrupting the router's bookkeeping.
  evidence: confirmed by direct inspection — `_current_frame`/`_current_screen_id` are plain instance attributes mutated at the end of `navigate()`, with no reentrancy guard. Not reachable today: no consuming screen calls `navigate()` from within its own `mount()`.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.navigate()`'s `previous_frame.destroy()` call is unguarded — if it raises (e.g. the frame's underlying Tk window is already gone), the following `current_screen_id`/`_current_frame` bookkeeping update never runs, leaving the router's internal state pointing at the just-torn-down screen while the new one is already visible and packed.
  evidence: confirmed by direct inspection — no `try`/`except` around `previous_frame.destroy()`, and the state-update lines execute strictly after it. Not reachable today: nothing external ever destroys a router-owned frame out of band.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-7-single-composition-root-screen-router.md`
  summary: `Router.register()` (`src/labyrinthes/app/router.py`) silently overwrites an existing `ScreenId` registration, and `Router.navigate()` always tears down and rebuilds the current screen even when re-navigating to the screen already mounted — neither behavior is tested or documented as intentional.
  evidence: confirmed by direct inspection — `register()` is a plain dict assignment (`self._mounts[screen_id] = mount`), and `navigate()` has no early-return guard comparing `screen_id` to `self._current_screen_id`. Not reachable today: `composition_root.build_app()` registers each `ScreenId` exactly once and no screen re-navigates to itself.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-7-single-composition-root-screen-router.md`
  summary: `tests/app/test_router.py`'s unregistered-screen test only verifies the previous screen's frame still exists (`winfo_exists()`) after a failed `navigate()`, not that it is still visually mapped (`winfo_ismapped()`) — a regression that merely unpacked (without destroying) the current frame on a failed navigate would pass this suite undetected.
  evidence: confirmed empirically that the seemingly-obvious fix doesn't directly apply — `winfo_ismapped()` returns `False` for any packed widget under this suite's own convention of an immediately-`withdraw()`-ed `tk_root`, so asserting it `True` would false-fail even on correct behavior; a real fix needs a different verification approach (e.g. checking the widget is still a child of the container / still gridded-or-packed via `pack_info()`), left open rather than patched blind.

## Deferred from: code review of spec-1-9-light-dark-theme-toggle-wired-end-to-end (2026-08-06)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `ThemeController(settings_repository)` is now constructed unconditionally on every real app launch (`composition_root.build_app()`), which is the first real, unconditional call site to actually reach `SettingsRepository.get()`/`JsonSettingsRepository.get()` on startup — escalating the already-deferred Story 1.5 finding that `read_setting_value` raises a raw `json.JSONDecodeError` on a malformed/truncated settings file from a hypothetical, "not reachable today" risk into a guaranteed app-launch crash the first time a corrupted `settings/shared/theme.json` exists (e.g. from the also-already-deferred Story 1.5 non-atomic-write finding leaving a half-written file after a crash).
  evidence: confirmed by direct inspection — `ThemeController._load_theme()` (this story) now catches `SettingNotFoundError`/`ValueError` but not `json.JSONDecodeError`, and `JsonSettingsRepository.get()`/`read_setting_value` still have no `try`/`except` around `json.load()` (unchanged since Story 1.5). Not patched here: fixing it means either `JsonSettingsRepository` raising a typed `LabyrinthesError` subclass on malformed JSON (a cross-cutting, repository-level fix already flagged in Story 1.5's own deferred entry as needing "a future format-validation or hardening pass") or every individual consumer defensively catching `json.JSONDecodeError`, which would leak a storage-adapter-specific exception type into `application`-layer callers. `ThemeController` was patched during this story's review to at least catch `ValueError` (an unrecognized-but-well-formed stored value) and default to `Theme.LIGHT`, closing the narrower half of this gap.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `ThemeController.toggle()` has no error handling around `self._settings.set(...)` — a persistence failure (disk full, read-only filesystem, permissions) propagates raw out of the theme-toggle `IconButton`'s Tk click callback, where Tkinter's default `report_callback_exception` only dumps a traceback to stderr; the user sees no feedback and the theme silently fails to change (though the current, in-memory `Theme` still flips and is applied to the re-rendered screen, so the visible UI and the failed-to-persist setting diverge until next restart).
  evidence: confirmed by direct inspection — `toggle()` calls `self._settings.set(...)` with no surrounding `try`/`except`. Same class as the already-deferred Story 1.4/1.5 "no error handling around repository writes" findings; not novel to this story, just reached by a new call site.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `build_app()`'s default `JsonSettingsRepository()` root (`DEFAULT_SETTINGS_ROOT = Path("settings")`, Story 1.5) is relative to the process's current working directory, with no XDG/home-directory anchoring — this story is the first to actually activate that default from a live, unconditional code path (theme persistence), so launching the real app from two different working directories now visibly and silently splits/duplicates the persisted theme (and will do the same for every other future `shared`-scope setting).
  evidence: confirmed by direct inspection — `settings_paths.py`'s `DEFAULT_SETTINGS_ROOT` is unchanged since Story 1.5; `composition_root.build_app()`'s new `settings_repository` parameter defaults to `JsonSettingsRepository()` with no root override. Pre-existing default, newly load-bearing.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `ThemeController.toggle()` iterates `self._listeners` with no isolation — if one subscriber's callback raises, every listener registered after it in `subscribe()` order never runs, even though the theme was already flipped and persisted before the loop started, leaving the persisted/in-memory `Theme` and the set of screens that actually re-rendered out of sync.
  evidence: confirmed by direct inspection — `toggle()`'s `for listener in self._listeners: listener(new_theme)` has no per-listener `try`/`except`. Not reachable today: exactly one listener (`composition_root.build_app()`'s `on_theme_change`) is ever registered.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: `composition_root.build_app()`'s `navigate()` closure sets `last_state` before calling `router.navigate(screen_id, state)`, not after it succeeds — if a future screen's `mount()` raises mid-construction, `last_state` is already updated to the new (never-actually-mounted) state while `router.current_screen_id` still points at the old, unchanged screen, so a subsequent theme toggle would re-navigate the old screen with the wrong state.
  evidence: confirmed by direct inspection of the `navigate()` closure's statement order. Same root cause as the already-deferred Story 1.7 finding "`Router.navigate()` has no exception-safety if a registered `mount()` callable raises mid-construction" — not reachable today for the identical reason (all three registered screens are static placeholders that never raise).
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: A theme-triggered re-navigate that raises (via `on_theme_change` → `navigate()` → `router.navigate()`, itself reachable only if a future screen's `mount()` raises) is not caught anywhere — `build_app()`'s own `try`/`except Exception: root.destroy(); raise` only wraps the initial construction phase, not later event-handler-triggered calls — so `ThemeController`'s in-memory/persisted theme would already reflect the new value while `Router` keeps the old frame mounted, and the exception would propagate uncaught out of the theme-toggle `IconButton`'s Tk click callback.
  evidence: confirmed by direct inspection — `on_theme_change`'s `navigate(current_screen_id, last_state)` call has no surrounding `try`/`except`, and `build_app()`'s own exception guard is scoped to the function body, not to callbacks invoked later from the Tk event loop. Same root cause and same "not reachable today" precondition as the two findings above and the pre-existing Story 1.7 entries on `Router.navigate()`'s lack of exception-safety.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-9-light-dark-theme-toggle-wired-end-to-end.md`
  summary: No test exercises the `on_theme_change` guard for "theme toggled before any `navigate()`" (`if current_screen_id is not None`) that this story's own spec I/O matrix calls out as an edge case — the guard is correct by inspection but not directly reachable through `build_app()`'s public surface (which always navigates to Home before returning), so a regression that deleted it would go undetected until an actual future entry point made it reachable.
  evidence: confirmed by direct inspection — `on_theme_change` is a local closure inside `build_app()`, not independently exposed for unit testing, and no existing test constructs a `ThemeController`/router pair in the pre-first-navigate state to exercise this branch.

## Deferred from: spec-1-10-accessibility-floor-keyboard-shortcut-consistency planning (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `settings_window.py`'s own docstring anticipates a "Shortcuts" category alongside Appearance/Ball/Difficulty, but no AC in Story 1.10 requires building real category-switching UI in `SettingsWindow`, so it was not added here.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/settings_window.py`'s module docstring ("Ball/Difficulty/Shortcuts don't exist as domain concepts yet (Epics 2/3, Story 1.10)"). This story's own intent contract explicitly forbids adding a "Shortcuts" category to `SettingsWindow` as unrequested scope; flagged here for whichever future story next adds real `SettingsWindow` content (e.g. Story 2.11 Appearance).
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: The dark-mode active-`ToolButton` text/background pairing (`colors.accent` `#3b82f6` on `colors.accent_bg` dark `#16233d`) measures ~4.25:1, marginally under the 4.5:1 AA text threshold, even though `DESIGN.md` explicitly asserts this pairing "was not flagged" (i.e. assessed as fine during the design phase).
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/tool_btn.py`'s `_apply_style()` (dark-mode active branch: `text_color = colors.accent`) against `DESIGN.md`'s locked color pairs. Not fixed here since it would mean introducing a third AA-fix token beyond the two `DESIGN.md` explicitly locks (`accent-on-tint`, `accent-strong-dark`), overriding a locked planning artifact's own stated decision unilaterally — this story's intent contract explicitly forbids touching it. Also not reachable today: no consuming screen renders an active `ToolButton` in production yet (only tests construct one directly).

## Deferred from: code review of spec-1-10-accessibility-floor-keyboard-shortcut-consistency (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: Pressing "B"/"P" (Home's canonical shortcuts) while a non-modal `SettingsWindow` is open silently closes it -- `bind_shortcut()` fires regardless of which widget has focus, triggering `Router.navigate()`, which destroys Home's `frame`; since `SettingsWindow` is a real Tk child `Toplevel` of that `frame` (`home/screen.py`'s `open_settings()`), it gets destroyed as a cascade side-effect.
  evidence: confirmed empirically (`frame.destroy()` after constructing a real `SettingsWindow(frame, ...)` leaves `settings.winfo_exists() == 0`), and by direct inspection of `home/screen.py`/`keybindings.py`. Not fixed here: this is the identical underlying mechanism -- `Router.navigate()`'s frame-destroy cascading into an open child `SettingsWindow` -- already identified and explicitly deferred twice before (Story 1.8's breadcrumb-click finding, Story 1.9's theme-toggle finding), both accepted with "`SettingsWindow` holds no real, stateful controls yet... no AC requires Settings to survive a navigation away." This story only adds a third trigger (a keyboard shortcut) for the same already-accepted tradeoff, not a new failure mode; revisit alongside the other two once `SettingsWindow` gains real persisted state worth protecting.
  Resolved by Story 1.11: `SettingsWindow` is now constructed as `SettingsWindow(parent, theme=theme)` (the app's persistent container) instead of `SettingsWindow(frame, theme=theme)` (the screen's own frame) in each screen's `open_settings()`, so it is no longer a Tk child of the frame `Router.navigate()` destroys -- it genuinely survives navigation now, across Home/Builder/Player, pinned by regression tests in each screen's test file plus one end-to-end test in `tests/app/test_composition_root.py`.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `bind_shortcut()` registers its handler via `widget.bind_all()` with no check on `event.widget`'s class, so a future screen that adds a text `Entry`/`Spinbox` whose typed content includes "b" or "p" would have those keystrokes hijacked into navigation instead of reaching the text field.
  evidence: confirmed by direct inspection of `keybindings.py`'s `bind_shortcut()` -- no widget-class guard exists. Not reachable today: no screen anywhere in the app has a text input yet (Home/Builder/Player are all still non-text-entry placeholders or button-only). Whichever future story first adds a text field to a screen that also registers a canonical shortcut needs to add this guard (e.g. skip firing when `event.widget` is an `Entry`/`Spinbox`/`Text`).
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `IconButton` snapshots `self._colors = colors_for(theme)` once at construction and reuses that snapshot for its focus-ring styling, while the three sibling widgets touched by this same story (`ToolButton`, `PillButton`, `Breadcrumb`) all recompute `colors_for(self._theme)` fresh inside their restyle methods -- a stylistic inconsistency introduced across four otherwise-parallel implementations of the identical focus-ring feature.
  evidence: confirmed by direct inspection of `icon_btn.py` vs `tool_btn.py`/`pill_btn.py`/`breadcrumb.py`. Harmless today: every screen fully re-mounts (a fresh widget instance, hence a fresh snapshot) on every theme toggle (Story 1.9's `Router.navigate()`-based re-render), so a stale cached `Theme` is never actually observable in practice. Worth aligning `IconButton` to the same recompute-fresh pattern as its siblings in a future cleanup pass, not urgent.

## Deferred from: follow-up code review of spec-1-10-accessibility-floor-keyboard-shortcut-consistency (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: Toggling `highlightthickness` between `RESTING_RING_THICKNESS` (1) and `FOCUS_RING_THICKNESS` (2) on focus changes the outer footprint of `PillButton`/`ToolButton`/`Breadcrumb` segments -- any focusable `common/` widget without a fixed size -- causing a ~2px layout shift in surrounding siblings each time focus enters or leaves; `IconButton` is unaffected only because its fixed `width`/`height` plus `pack_propagate(False)` absorb the change.
  evidence: confirmed empirically -- a naturally-sized `tk.Frame`'s reported width grows from 98px to 100px when `highlightthickness` is bumped 1->2 with no other geometry constraint, and `PillButton`/`ToolButton` both use plain `tk.Frame` with no fixed width/height. Not fixed here: avoiding the shift needs a different mechanic than "thicker highlight ring" (e.g. always reserving 2px of highlight space and toggling only its color), which the Design Notes explicitly ruled out for `ToolButton` -- color alone can't distinguish active-unfocused from active-focused there, per the module's own documented rationale. A genuine redesign trade-off, not a mechanical fix, and no AC requires layout stability under focus.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `bind_shortcut()`'s `_current_registration` registry keys registrations on `id(widget.tk)` as a stand-in for "which Tk interpreter", a CPython object-identity shortcut that a later, unrelated `tk.Tk()` instance could reuse once an earlier one is garbage-collected.
  evidence: confirmed by direct inspection -- `interpreter_id = id(widget.tk)` (`keybindings.py`) has no stronger identity guarantee than CPython's `id()` semantics. Not reachable today in the real app (exactly one `Tk()` root lives for the whole process) or in the current test suite (each `tk_root`/child-widget fixture's `<Destroy>` cleanup removes its registry entries synchronously before the next `tk.Tk()` is constructed), but a future fixture that constructs/destroys `Tk()` instances without waiting for teardown would need a sturdier identity than `id()`.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `bind_shortcut()`'s `.lower()`/`.upper()`-based sequence construction (and `Keybinding.event`/`.display`) silently assumes every `key` is a single ASCII letter; a future `Keybinding` using a function-key name (e.g. `"F1"`) would produce a syntactically valid but semantically wrong Tk sequence (`<KeyPress-f1>` is not a real X11 keysym) that would simply never fire, with no error raised anywhere.
  evidence: confirmed by direct inspection -- `bind_shortcut()` and `Keybinding.display`/`.event` all treat `key` as case-invertible with no validation. Not reachable today: `KEYBINDINGS` only holds `"b"`/`"p"`, both single ASCII letters. Worth a guard (e.g. `len(key) == 1 and key.isalpha()`) or a documented constraint whenever a future story adds a non-letter shortcut.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `IconButton`/`ToolButton`/`PillButton` all now set `takefocus=True` unconditionally in `__init__`, even when constructed with `command=None`, creating a keyboard tab-stop that does nothing on Enter/Space.
  evidence: confirmed by direct inspection of all three widgets' `__init__` plus their shared `_on_click()` no-op-when-`command`-is-`None` pattern. Not reachable today: every current call site (`top_bar.py`'s two `IconButton`s, `home/screen.py`'s two `PillButton`s) passes a real `command`; would only surface once a future screen constructs one of these widgets purely decoratively.
- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-10-accessibility-floor-keyboard-shortcut-consistency.md`
  summary: `IconButton`/`ToolButton`/`PillButton`/`Breadcrumb`'s new `<Return>`/`<space>` bindings fire on every `KeyPress`, so holding the key down triggers the OS's key-auto-repeat and re-invokes the command/navigation/`ToolButtonGroup.activate()` repeatedly for as long as it's held -- a mouse click can't do this, since `<Button-1>` only fires once per physical press.
  evidence: confirmed by direct inspection -- all four widgets bind their activation handler to `<Return>`/`<space>` (`KeyPress`), not `<KeyRelease>` or any auto-repeat guard. Low impact for the current idempotent handlers (`Router.navigate()` to the same screen, `ToolButtonGroup.activate()` on an already-active button), but worth a debounce (e.g. binding `<KeyRelease>` instead, or tracking key-down state) before a future non-idempotent command is wired to one of these widgets.

## Deferred from: code review of 1-11-settings-dialog-survives-screen-navigation (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-11-settings-dialog-survives-screen-navigation.md`
  summary: `open_settings()` has no guard against opening multiple `SettingsWindow` instances -- repeated settings-icon clicks stack independent `Toplevel`s with no dedup, across all three screens (`home/screen.py`, `builder/screen.py`, `player/screen.py`).
  evidence: confirmed by direct inspection -- each `open_settings()` closure unconditionally calls `SettingsWindow(...)` with no check for an already-open instance. Not fixed here: the construction call's target widget changed (`parent` instead of `frame`, this same story's real fix), but the missing dedup guard predates this diff and is out of its scope. Revisit if/when `SettingsWindow` gains real state where stacked duplicate dialogs would be confusing or lossy -- worth prioritizing a little sooner than before, now that `SettingsWindow` survives navigation and so can accumulate across more of them than a closing one ever could.

## Deferred from: code review of 1-12-persistence-hardening-atomic-writes-typed-errors (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/epic-1/spec-1-12-persistence-hardening-atomic-writes-typed-errors.md`
  summary: `read_maze_csv`'s `path.read_text(encoding="utf-8")` call and `read_setting_value`'s `json.load(handle)` call can each raise a raw `UnicodeDecodeError` on invalid-UTF-8/binary content -- not a `LabyrinthesError` subclass, so it isn't caught by either function's new typed-error wrapping (`MazeCorruptError`/`SettingCorruptError`).
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/storage/csv_maze_format.py:44` (the `read_text` call sits before the `try` block that wraps only header parsing) and `src/labyrinthes/adapters/storage/settings_format.py:28-32` (`json.load` reads the handle internally, so a decode error surfaces from inside the `try` but isn't a `json.JSONDecodeError` subclass, so the `except` clause doesn't catch it). Pre-existing gap -- this code path had zero error handling before this story's diff -- and narrower than Task 2/Task 3's explicitly stated scope (header-parsing / `json.JSONDecodeError` only), so not a regression introduced here. Worth widening the guard (or adding `UnicodeDecodeError` to each except tuple) whenever a story next touches either reader, since `MazeCorruptError`'s own docstring already claims the broader "content can't be parsed into a Maze" intent.

## Deferred from: code review of 2-1-player-maze-selection-screen-classic-maze-browsing (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-1-player-maze-selection-screen-classic-maze-browsing.md`
  summary: Toggling the theme while browsing the classic-maze gallery silently resets the browse position back to the first maze, because `composition_root.py`'s theme-toggle listener re-navigates Player with `last_state` (which stays `None` while merely browsing -- it's only ever set to a `Maze` by the "Play" confirm action), and `ClassicMazeGallery`'s internal `_index` isn't part of that `state` channel.
  evidence: reproduced directly -- browse to "Classic Maze 3 of 3", trigger the theme toggle, and the re-mounted `ClassicMazeGallery` starts over at index 0. Not fixed here: the clean fix is architectural (some way to preserve a screen's internal state across a theme-driven remount, distinct from a genuine navigation), which is bigger than this widget alone and will recur for every future stateful Player view -- Story 2.4's gameplay screen carries far more such state (ball position, elapsed time, Level/Difficulty selection) that would hit the exact same issue. Worth solving once, holistically, rather than per-widget; revisit when Story 2.4 lands or sooner if this proves more annoying in practice than expected.

## Deferred from: code review of spec-2-2-random-maze-generation-with-validation (2026-08-09)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-2-random-maze-generation-with-validation.md`
  summary: `ClassicMazeGallery._on_generate_random` has no guard against opening multiple `GenerateRandomDialog` instances -- repeated "N" presses (or clicking "Generate random" again while one is already open) stack independent, non-modal `Toplevel`s with no dedup.
  evidence: confirmed by direct inspection -- `_on_generate_random` unconditionally constructs a new `GenerateRandomDialog(self, ...)` with no check for an already-open instance, and the dialog never calls `grab_set()`/`transient()`. Not fixed here: this is the exact same gap already identified and deliberately deferred for `SettingsWindow`'s `open_settings()` across all three screens (see this file's "Deferred from: code review of 1-11-settings-dialog-survives-screen-navigation" entry above) -- same underlying pattern (a non-modal dialog constructor with no dedup guard), just a second, independent instance of it introduced by this story's new dialog. Revisit alongside that entry rather than as a one-off patch scoped to this widget alone.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-2-random-maze-generation-with-validation.md`
  summary: `application/maze_size_bounds.py`'s `_read_bound` calls `int(value)` on whatever `SettingsRepository.get()` returns, so a stored JSON float (e.g. `3.9`) silently truncates to `3` instead of being treated as a corrupt/malformed value and falling back to the default.
  evidence: confirmed by direct inspection -- `int(3.9) == 3` succeeds with no exception, so the existing `except (..., ValueError, TypeError)` fallback never triggers for this case. Not reachable today: nothing in the shipped app ever writes to the four `MAZE_MIN/MAX_*` shared keys yet (`read_maze_size_bounds` is read-only, per its own docstring, and no Builder "New Maze" settings UI exists yet to write them either) -- only a hand-edited settings file could produce this. Worth an explicit `isinstance(value, int)` guard whenever a future story adds a writer for these keys.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-2-random-maze-generation-with-validation.md`
  summary: `read_maze_size_bounds` places no upper sanity ceiling on a stored `max_columns`/`max_rows` beyond "positive and `min <= max`" -- a very large stored value (e.g. `100000`) would be accepted as-is, and `generate_random_maze`'s carve runs synchronously on the Tk main thread with no size cap, so confirming such a maze would freeze the UI for the duration of generation.
  evidence: confirmed by direct inspection -- `_read_bound`'s only checks are "parses as int" and "`>= 1`"; `read_maze_size_bounds`'s only cross-field check is pair inversion (this story's own fix, see above), neither caps the value against `DEFAULT_MAZE_SIZE_BOUNDS`'s own ceiling. Not reachable today for the same reason as the float-truncation entry above -- no writer exists yet for these keys, only a hand-edited settings file could produce an oversized value; the FR-4-mandated UI path (the dialog itself) already gates columns/rows to the resolved bounds via `validate_dimensions` before `generate_random_maze` is ever called. Worth a hard ceiling clamp (e.g. never resolve above `DEFAULT_MAZE_SIZE_BOUNDS.max_columns`/`.max_rows`) if a future story adds a settings UI that could otherwise let a user configure this by mistake.

## Deferred from: code review of spec-2-3-random-maze-saving-reappears-after-restart (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-3-random-maze-saving-reappears-after-restart.md`
  summary: `SaveMazeDialog` never calls `grab_set()`/`transient()` and `GameplayPlaceholder._on_save_clicked` has no guard against opening a second instance while one is already open -- the underlying screen stays fully interactive (Home/global shortcuts can destroy the placeholder mid-edit, cascading into the open dialog and silently discarding an unconfirmed save), and a double Save click stacks two dialogs, the second holding a stale `existing_names` snapshot taken before the first one's save.
  evidence: confirmed by direct inspection -- `SaveMazeDialog.__init__` never calls `grab_set()`/`transient()`/`wait_window()`, and `_on_save_clicked` unconditionally constructs a new `SaveMazeDialog(self, ...)` with no check for an already-open instance. This is the exact same gap already identified and deliberately deferred for `SettingsWindow`'s `open_settings()` (see this file's "Deferred from: code review of 1-11-settings-dialog-survives-screen-navigation" entry) and `GenerateRandomDialog` (see "Deferred from: code review of spec-2-2-random-maze-generation-with-validation") -- a third, independent instance of the same non-modal-dialog-with-no-dedup-guard pattern. Revisit alongside those entries rather than as a one-off patch scoped to this dialog alone.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-3-random-maze-saving-reappears-after-restart.md`
  summary: `SaveMazeDialog._on_save_clicked` does not catch an exception from `on_confirm` (e.g. `GameplayPlaceholder._on_save_confirmed` calling `maze_repository.save()`, which could raise on a disk I/O error) -- the dialog has already destroyed itself by that point, so the failure surfaces with no visible error message and `GameplayPlaceholder._maze` is left in whatever partial state the failed call produced.
  evidence: confirmed by direct inspection -- no `try`/`except` wraps the `self._on_confirm(name)` call, consistent with this codebase's existing convention of not catching domain/storage errors at the UI layer (e.g. `ClassicMazeGallery._on_generation_confirmed` doesn't catch `generate_random_maze` either). Not a regression introduced by this story specifically -- a pre-existing, codebase-wide posture -- but worth a holistic pass (e.g. a shared inline-error-surfacing convention for `application/`-raised errors reaching `adapters/tkinter/`) whenever a story first needs it for real, rather than solving it once per dialog.

## Deferred from: follow-up review of spec-2-3-random-maze-saving-reappears-after-restart (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-3-random-maze-saving-reappears-after-restart.md`
  summary: `SaveMazeDialog`'s "s"/"S" guard (see the already-deferred no-modality/no-dedup entry above) only intercepts the keystroke while the name `Entry` itself has focus -- tabbing to the Save/Cancel `PillButton`s, or clicking elsewhere inside the dialog, then pressing "s" leaves nothing to consume it locally, so the global `bind_all()` `save_maze` shortcut still fires and stacks a second `SaveMazeDialog`; confirming the first one then destroys the second (and its unsaved text) as a side effect of `GameplayPlaceholder._build()`'s child-teardown loop.
  evidence: confirmed by direct inspection of `save_maze_dialog.py` (the `<KeyPress-s>`/`<KeyPress-S>` binds are only on `self._name_entry`, not on the `Toplevel` or its buttons) and `gameplay_placeholder.py`'s `_build()` (`for child in self.winfo_children(): child.destroy()` has no per-dialog exemption). Same root cause and same fix (`grab_set()`/`transient()` or a dedup guard) as the already-deferred entry above; recorded separately because the trigger path (focus moved off the name field, not a keystroke while typing) and the concrete consequence (a *second* dialog's unsaved input silently destroyed, not just a stacked window) are more specific than that entry captures.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-3-random-maze-saving-reappears-after-restart.md`
  summary: `SaveMazeDialog`'s collision check (`name in self._existing_names`) is case-sensitive, and `existing_names` is a snapshot of `maze_repository.list_names(MazeKind.SAVED_RANDOM)` taken once when the dialog opens, never re-checked at confirm time -- so (a) on a case-insensitive filesystem (default macOS/Windows), saving "Forest" when "forest" already exists shows no overwrite warning even though both names resolve to the same on-disk file, and (b) a name saved by some other path between dialog-open and confirm would be silently overwritten with no warning either.
  evidence: confirmed by direct inspection -- `_on_save_clicked`'s `name in self._existing_names` is a plain, case-sensitive `in` check against a `list[str]` captured once in `GameplayPlaceholder._on_save_clicked` and never refreshed; `adapters/storage/paths.py::maze_file_path` (Story 1.4) does no case folding either, so the UI-side check is at least internally consistent with the storage layer's own case-sensitivity, not a new inconsistency this story introduced. The stale-snapshot half is low-probability today (nothing else can write a `SAVED_RANDOM` maze while this dialog is open, in the current single-window flow) but would become live once a second save path exists (e.g. Story 3.6, or a future Builder/Player-open-simultaneously flow per this codebase's existing cross-launch linkage).
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-3-random-maze-saving-reappears-after-restart.md`
  summary: `SaveMazeDialog`'s name-shape validation (`_validate_name`) has no upper length ceiling -- an arbitrarily long name passes validation and reaches `maze_repository.save()`, where `CsvMazeRepository`'s `maze_file_path` (Story 1.4) has no length check either, so a name exceeding the filesystem's max filename length would fail at the storage layer with a raw, unhandled `OSError` rather than the dialog's own inline error.
  evidence: confirmed by direct inspection of `_validate_name` (only checks empty/path-separator) and `adapters/storage/paths.py::maze_file_path` (no length check). Mirrors the already-deferred Story 1.4 pattern of `CsvMazeRepository`'s writers not validating input shape beyond what the domain layer requires; not a new gap this story introduced so much as this story's new UI-side validation not going further than the storage layer it fronts.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-3-random-maze-saving-reappears-after-restart.md`
  summary: `ClassicMazeGallery._current_maze()`'s unguarded `maze_repository.load(name, kind)` call (pre-existing since Story 2.1, for `CLASSIC` mazes) now also covers `SAVED_RANDOM` entries this story added to the same pager -- if a listed maze's backing file is deleted or renamed on disk after the gallery's `self._entries` listing was built (e.g. edited externally, or a future delete feature), `_refresh_display()`/`_on_play()` raises an unhandled `MazeNotFoundError` instead of showing an inline message.
  evidence: confirmed by direct inspection -- `_current_maze()` calls `self._maze_repository.load(name, kind)` with no `try`/`except`, unchanged from Story 2.1's original `CLASSIC`-only implementation. Not a new failure class this story introduced -- the same gap already existed for hand-edited/removed classic-maze files -- but this story widens its reachable surface to include user-authored `SAVED_RANDOM` mazes, which are materially more likely to be deleted/renamed by the user (e.g. cleaning up saved mazes outside the app) than the shipped classic set.

## Deferred from: code review of spec-2-4-gameplay-screen-foundation-rendering-hud-baseline-movement-win-detection (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-4-gameplay-screen-foundation-rendering-hud-baseline-movement-win-detection.md`
  summary: `SaveMazeDialog._name_entry`'s pre-existing "s"/"S" `<KeyPress>` `"break"` guard (Story 2.3) does not just suppress the global `save_maze` shortcut -- it also prevents the letter "s"/"S" from ever being typed into the field at all, since an instance-level `"break"` stops Tk's bindtag scan before the `Entry`'s own *class* binding (which performs self-insert) ever runs.
  evidence: confirmed live via a synthetic `<KeyPress>` event with `keysym="s"` dispatched at `_name_entry` -- the field's content stays `''` afterward, with the guard installed. Pre-existing since Story 2.3 (the guard's own two review passes only tested it via `.insert()`, never a real keystroke, so this was never observable to the test suite); Story 2.4 did not introduce or touch these two bindings, only discovered the same underlying idiom's failure mode while extending the pattern for its own movement shortcuts (see this story's own fix: `GameplayScreen._on_move` now guards via `self.focus_get()` instead of a per-`Entry` "break" binding, specifically to avoid this exact class of bug for the four new movement keys). Fixing the pre-existing "s"/"S" case the same way is a small, self-contained follow-up, not attempted here to keep this story's diff scoped to its own new shortcuts.
- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-4-gameplay-screen-foundation-rendering-hud-baseline-movement-win-detection.md`
  summary: `Maze.__post_init__` only requires `entry`/`exit` to be valid `Grid.cell_at()` addresses, which accepts the closed-border padding row/column (`row == height` or `col == width`), not just the playable `[0, width) x [0, height)` region -- a malformed/hand-edited classic maze CSV with such an entry/exit can crash `domain.movement.attempt_move` (a DOWN/RIGHT step off an already-out-of-range position indexes past the padding row/column `attempt_move` itself defends against, but never validates the *starting* position it's given) or render the entry/exit marker/ball partly or fully outside `MazeCanvas`'s visible canvas area.
  evidence: confirmed by inspection -- `Maze.__post_init__` (`domain/maze.py`) calls `self.grid.cell_at(self.entry)`/`cell_at(self.exit)`, which is deliberately looser than "playable" (see `Grid.cell_at`'s own docstring: "`position.row == height` or `position.col == width` are valid, in-range accesses"); classic mazes load from arbitrary CSV (Story 1.4) with no additional range check. Inert before this story (`GameplayPlaceholder` only ever printed a text summary); Story 2.4's new `PlayerSession`/`MazeCanvas` are the first code to actually compute pixel coordinates and collision-check against `maze.entry`/`maze.exit`, making this newly reachable. A second, related gap in the same area: `Maze` also never forbids `entry == exit` outright, so `start_session` doesn't detect an already-solved maze at mount (the win banner only appears after the first move confirms the equality), and that first move -- even one blocked by a wall, since a blocked move's "new" position equals the unchanged current position -- would incorrectly mark the session solved. Both are Story 1.1/1.4-era domain-modeling gaps surfaced incidentally by this story's review, not introduced by it; a single shared fix (validating `entry`/`exit` lie within the playable region and differ from each other, in `Maze.__post_init__`) would close both at once.

## Deferred from: follow-up review of spec-2-4-gameplay-screen-foundation-rendering-hud-baseline-movement-win-detection (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-4-gameplay-screen-foundation-rendering-hud-baseline-movement-win-detection.md`
  summary: `MazeCanvas._cell_size` (`src/labyrinthes/adapters/tkinter/player/maze_canvas.py`) divides by `width`/`height` with no zero-guard -- a domain-legal degenerate `Grid` (playable `width == 0` or `height == 0`, which `Grid.__post_init__` alone permits; only `Grid.filled`'s own separate guard forbids it) crashes `GameplayScreen`/`MazeCanvas` construction with a raw `ZeroDivisionError` instead of a handled error.
  evidence: confirmed by direct inspection -- `_cell_size()` computes `_MAX_CANVAS_SPAN // width` / `// height` unconditionally, and `Grid.__post_init__` only requires `cells` to be non-empty and rectangular (`domain/grid.py`), not `width > 0 and height > 0`; that stronger check lives solely in the separate `Grid.filled()` factory, which a maze loaded straight from a malformed classic-maze CSV (Story 1.4's `read_maze_csv`) never goes through. Same root cause class as this file's adjacent, already-deferred `Maze.__post_init__` entry/exit validation gap (a malformed classic-maze CSV reaching newly-reachable Story 2.4 rendering/movement code) but a distinct symptom (a crash in cell-size computation, not a bad `entry`/`exit` position) -- worth folding into the same future `Maze`/`Grid` construction-time validation pass rather than a narrow one-off guard in `MazeCanvas` alone.

## Deferred from: code review of spec-2-8-hard-mode-invisible-ball-fog-overlay-status-light (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-8-hard-mode-invisible-ball-fog-overlay-status-light.md`
  summary: No Settings color-picker UI exists for the two HARD-mode status-light colors -- `SettingsWindow` remains the Story 1.8/1.11 "Appearance coming soon" placeholder, and no story in Epics 1-5 owns a color picker. Story 2.8 ships the `read_hard_mode_*`/`write_hard_mode_*` readers/writers (the `game`-scoped persistence seam) plus read-on-activation (`_hard_mode_colors()`), so AC-4 is exercised at the repository seam by tests; the writers are not yet called by any UI. The future color picker (whichever story first adds real `SettingsWindow` content, e.g. Story 2.11) consumes the writers, and the HARD control's stable sidebar "Mode" group location is kept so Story 5.5's first-activation ⓘ-anchor can attach to it.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/settings_window.py` (placeholder only) and `src/labyrinthes/application/hard_mode_settings.py` (writers never invoked from `adapters/tkinter/player/gameplay_screen.py`), plus the story's own explicitly-deferred Settings-UI decision. No AC in Story 2.8 requires a picker; exercising it from the repository seam is the documented mechanism.

## Deferred from: code review of spec-2-9-timer-optional-time-limit-timeout-message (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/epic-2/spec-2-9-timer-optional-time-limit-timeout-message.md`
  summary: No Settings time-limit picker UI exists -- `SettingsWindow` remains the Story 1.8/1.11 "Appearance coming soon" placeholder, and no story in Epics 1-5 owns a time-limit picker. Story 2.9 ships the `read_time_limit`/`write_time_limit` reader/writer (the `game`-scoped persistence seam) plus read-on-mount (`_time_limit`), so AC-3 is exercised at the repository seam by tests that `write_time_limit` a value and drive a fake-elapsed `_on_tick`; the writer is not yet called by any UI. The future time-limit picker (whichever story first adds real `SettingsWindow` content, e.g. Story 2.11) consumes the writer, and the `0`-sentinel on-disk encoding (`write_time_limit(None)` persists `0`) is the documented "no limit" representation.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/settings_window.py` (placeholder only) and `src/labyrinthes/application/time_limit_settings.py` (writer never invoked from `adapters/tkinter/player/gameplay_screen.py`), plus the story's own explicitly-deferred Settings-UI decision. No AC in Story 2.9 requires a picker; exercising it from the repository seam is the documented mechanism (same pattern as Story 2.8's HARD-color picker deferral).

## Deferred from: review loop (bad_spec) of spec-3-1-new-maze-open-a-sketch (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: "Open a Sketch" -- letting the user reopen and continue editing an existing sketch maze from the Home screen -- needs its own story. Story 3.1's spec title/Problem statement originally bundled it in, but the spec's actual Boundaries/Code Map/I-O Matrix/Tasks were only ever scoped to "New Maze"; the two are independently shippable deliverables per the SCOPE STANDARD and should not share one spec. A prior work session had already committed a first attempt, `src/labyrinthes/adapters/tkinter/common/open_sketch_dialog.py` (`OpenSketchDialog`, exported from `common/__init__.py`), but it was never wired into any screen and this review loop reverted it as out-of-scope. Whoever picks up "Open Sketch" next should NOT reuse that reverted implementation as-is -- it had real bugs, listed below -- but its overall shape (a `tk.Toplevel` reading sketch names via `MazeRepository.list_names(MazeKind.SKETCH)` into a dropdown, loading the selection via `MazeRepository.load()`, `on_confirm: Callable[[Maze], None]`) is a reasonable starting point.
  evidence: confirmed by three parallel review layers (Blind Hunter, Edge Case Hunter, Verification Gap) against the diff since baseline `753211b`. Concrete bugs found in the reverted `open_sketch_dialog.py`, to avoid repeating: (1) `_compute_errors` had a duplicated, dead `hasattr(self, "_no_sketch_label")` guard that made its "No sketches available." message unreachable, leaving Confirm wrongly enabled with an empty selection and calling `maze_repository.load("", MazeKind.SKETCH)`; (2) no `trace_add` on the sketch-selection `StringVar`, so Confirm never re-validated after the user picked a different sketch following a failed load; (3) a bare `except Exception as e` leaked raw exception text (`f"Failed to load sketch: {e}"`) straight into the UI; (4) `tk.OptionMenu` was built with no `colors`/`TYPOGRAPHY` styling, unlike every other widget in the file; (5) used `ttk.Button` + disabled-state gating instead of this codebase's established `PillButton` + "leave errors visible, no state change" dialog pattern (see `NewMazeDialog`/`GenerateRandomDialog`); (6) zero test coverage. Also needs: `home/screen.py`'s `mount()` gaining a `maze_repository: MazeRepository` keyword-only parameter (parallel to the existing `settings_repository` one) so it can construct `OpenSketchDialog`, plus an "Open Sketch" `PillButton`/keybinding entry point and tests mirroring the "New Maze" ones.

## Deferred from: follow-up code review of spec-3-1-new-maze-open-a-sketch (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: `NewMazeDialog` has no dedup guard against opening a second instance -- repeated "New Maze" clicks (or the `C` shortcut) while one is already open stack independent, non-modal `Toplevel`s with no dedup.
  evidence: confirmed by direct inspection -- `go_to_new_maze()` (`src/labyrinthes/adapters/tkinter/home/screen.py`) unconditionally constructs a new `NewMazeDialog(...)` with no check for an already-open instance, and the dialog never calls `grab_set()`/`transient()`. Same already-repeatedly-deferred gap as `SettingsWindow`'s `open_settings()`, `GenerateRandomDialog`, and `SaveMazeDialog` (see this file's Story 1.11/2.2/2.3 entries above) -- a fourth independent instance of the identical pattern, not novel to this story. Revisit alongside those entries rather than as a one-off patch.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: `NewMazeDialog._bounds` is read once via `read_maze_size_bounds(...)` at construction and never refreshed -- if the user leaves the dialog open (it's non-modal, so Settings stays fully reachable) and changes the maze-size bounds via a future Settings UI, the dialog keeps validating and building against stale bounds.
  evidence: confirmed by direct inspection -- `self._bounds` is set once in `__init__` and only read afterward, with no listener on the settings repository. Mirrors `GenerateRandomDialog`'s (Story 2.2) identical construction-time-only `self._bounds` capture -- a pre-existing pattern, not a new gap. Not reachable today: no shipped Settings UI can write the four `MAZE_MIN/MAX_*` keys yet (per the already-deferred Story 2.2 entry above).
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: If `read_maze_size_bounds` ever resolved `min_columns == 1` and `min_rows == 1` (only reachable via a hand-edited/corrupt settings file, since no shipped writer exists for these keys and `DEFAULT_MAZE_SIZE_BOUNDS` is `(3, 50, 3, 35)`), confirming at 1x1 builds `Maze(entry=Position(0,0), exit=Position(0,0), ...)` -- `Maze.__post_init__` only bounds-checks `entry`/`exit` via `Grid.cell_at()`, it never rejects `entry == exit`.
  evidence: confirmed by direct inspection of `new_maze_dialog.py`'s `_on_confirm_clicked` (`exit=Position(row=rows - 1, col=columns - 1)`, degenerates to `(0, 0)` when `rows == columns == 1`) and `domain/maze.py`'s `Maze.__post_init__` (no entry-versus-exit distinctness check). Same root-cause class as the already-deferred Story 2.4 entry "`Maze` also never forbids `entry == exit` outright" -- not a new domain gap, just a second UI-side path that could theoretically reach it, and gated behind the same "not reachable today, no writer exists" precondition as the Story 2.2 bounds-corruption entries above.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: `NewMazeDialog._on_confirm_clicked` calls `self._on_confirm(maze)` then unconditionally `self.destroy()` right after, with no `try`/`except` around the callback -- if `on_confirm` (i.e. `navigate(ScreenId.PLAYER, maze)`, which can itself raise inside `Router.navigate()`'s screen-mount call) raises, the dialog is left open holding a maze that has already been handed to the caller, and the exception propagates raw out of a Tk button callback.
  evidence: confirmed by direct inspection -- no `try`/`except` wraps `self._on_confirm(maze)`. Same class as the already-deferred Story 2.3 entry "`SaveMazeDialog._on_save_clicked` does not catch an exception from `on_confirm`" -- a repeat of this codebase's existing, already-accepted posture of not catching domain/storage/navigation errors at the dialog-confirm layer, not a new gap introduced by this story.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: `NewMazeDialog`'s two `tk.Entry` fields only re-validate on `<KeyRelease>`; a mouse-driven paste (middle-click or right-click "Paste") into either field doesn't fire `<KeyRelease>`, so the inline error label stays stale until the next keystroke or a Create click.
  evidence: confirmed by direct inspection -- `_add_field` binds only `<KeyRelease>`/`<Return>`, no `<<Paste>>`. Mirrors `GenerateRandomDialog`'s identical `<KeyRelease>`-only binding (Story 2.2) -- a pre-existing, systemic pattern across every field-validated dialog in `common`/`player`, not novel to this story.

## Deferred from: code review of spec-3-1-new-maze-open-a-sketch (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: The spec's Iteration 1 Spec Change Log justifies omitting `transient()`/`grab_set()` from `NewMazeDialog` by citing `confirm_dialog.py`'s module docstring as evidence that non-modal dialogs in this codebase never call `transient()` -- but `confirm_dialog.py` only documents non-modal as "no `grab_set()`", and its `__init__` does call `self.transient(parent)`. `NewMazeDialog`'s actual behavior is not wrong -- it correctly mirrors its real declared model, `GenerateRandomDialog`, which also omits `transient()` -- so no code change is needed here, only the spec's citation is inaccurate.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/common/confirm_dialog.py:113` (`self.transient(parent)` present) versus `src/labyrinthes/adapters/tkinter/player/generate_random_dialog.py` and `src/labyrinthes/adapters/tkinter/common/new_maze_dialog.py` (neither calls `transient()`). Pre-existing inconsistency across this codebase's dialogs (`ConfirmDialog` alone calls `transient()`), not introduced by this story.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-1-new-maze-open-a-sketch.md`
  summary: `final_revision` is left blank (`''`) in this spec's frontmatter even though the Spec Change Log documents two completed review iterations with concrete patches applied.
  evidence: confirmed by direct inspection of the frontmatter (`final_revision: ''`) versus the two dated, resolved iterations recorded in the same file's Spec Change Log section. Cosmetic bookkeeping gap, not a functional issue.

## Deferred from: code review of spec-3-2-wall-editing-break-restore (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-2-wall-editing-break-restore.md`
  summary: A keyboard-only user has no way to *restore* an already-broken wall -- Break mode's toggle is click-only, and Pass-through's cursor movement only ever breaks a wall it crosses, never re-closes one. The epic's NFR6 ("every Builder action is keyboard-reachable") is not fully met for wall restoration specifically.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/builder/screen.py` (`_BuilderMazeCanvas._on_click` is the only entry point into `apply_wall_toggle`, wired to a mouse `<Button-1>` binding, no keyboard equivalent) and `src/labyrinthes/application/builder_session.py`'s `move_cursor` (Pass-through branch only ever calls `break_wall`, never `restore_wall`/`toggle_wall`). The frozen spec's own Intent explicitly scoped Break mode as click-based ("single-click Break mode") with Pass-through as the keyboard-reachable break-only alternative, so this is a known characteristic of the epic's FR-1 design rather than an oversight introduced by this story's implementation -- worth a dedicated accessibility pass in a later story (e.g. a keyboard-driven wall-cursor-select shortcut).
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-2-wall-editing-break-restore.md`
  summary: `_BuilderMazeCanvas`'s `_cell_size` clamp (`clamp(min(480 // width, 480 // height), 16, 40)`) can render a canvas wider/taller than its own 480px budget for extreme aspect ratios (e.g. a 50x3 grid clamps to 16px cells, an 800px-wide canvas), with no scrollbar or further containment.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/builder/screen.py`'s `_cell_size` and `src/labyrinthes/adapters/tkinter/player/maze_canvas.py`'s identically-shaped `_cell_size` (same `_MIN_CELL_SIZE`/`_MAX_CELL_SIZE`/`_MAX_CANVAS_SPAN` clamp, same lack of a scrollbar). Story 3.2 faithfully mirrors this already-shipped Story 2.4 pattern per the Boundaries ("Builder-specific widgets stay local to `adapters/tkinter/builder/`"); not a new gap introduced by this story.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-2-wall-editing-break-restore.md`
  summary: `_BuilderMazeCanvas.__init__` (and `player/maze_canvas.py`'s identical `_cell_size`) divides by `grid.width`/`grid.height` with no zero-guard -- a `Maze` whose `Grid` has `width == 0` or `height == 0` (permitted by `Grid.__post_init__`, which only validates non-empty/rectangular `cells`, not positive dimensions; only `Grid.filled()` rejects `<= 0`) would raise a raw `ZeroDivisionError` instead of a domain error.
  evidence: confirmed by direct inspection of `_cell_size`'s `480 // width` (`src/labyrinthes/adapters/tkinter/builder/screen.py`) and the identical unguarded computation in `src/labyrinthes/adapters/tkinter/player/maze_canvas.py` (Story 2.4, pre-existing). Not reachable via `NewMazeDialog` (its bounds validation and `Grid.filled()` both reject non-positive dimensions) or any currently-shipped save/load path; only a hand-corrupted maze file could construct a degenerate `Grid` directly. Same pre-existing gap already present in Player's identical code, not introduced or worsened by this story.

## Deferred from: code review of spec-3-3-zone-editing-destroy-restore-a-rectangular-zone (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-3-zone-editing-destroy-restore-a-rectangular-zone.md`
  summary: A keyboard-only user has no way to perform a zone destroy/restore at all -- Destroy Zone/Restore Zone are selectable via the `d`/`r` keybindings, but the rectangle gesture itself is mouse-drag-only (`<Button-1>`/`<ButtonRelease-1>`), with no keyboard equivalent (e.g. a mark-corner/commit-zone shortcut using the existing cursor). This is the *second* NFR6 keyboard-accessibility gap surfaced in the Builder by a code review (the first was Story 3.2's "no keyboard way to restore a wall", deferred above) -- per the epic-1/epic-2 retrospective action item on re-deferred items, this should be flagged for scheduling rather than deferred a third time.
  evidence: confirmed by direct inspection of `src/labyrinthes/adapters/tkinter/builder/screen.py` (`_BuilderMazeCanvas._on_click`/`_on_release` are the only entry points into `apply_zone_operation`, both mouse-bound; existing arrow-key `_on_move` never wires into zone-corner selection) and Epic 3's context ("Accessibility floor (NFR6): every Builder action is keyboard-reachable"). The frozen spec's Intent/Approach never scoped a keyboard alternative for the drag gesture, so this is a known characteristic of the story's approved design rather than an implementation defect -- worth a dedicated accessibility story covering both this and the Story 3.2 wall-restore gap together.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-3-zone-editing-destroy-restore-a-rectangular-zone.md`
  summary: `destroy_zone`/`restore_zone` perform one full `Grid.cells` tuple rebuild per wall in the spanned rectangle (via `wall_editing.break_wall`/`restore_wall`'s `_with_wall_bit`) rather than a single combined rebuild for the whole zone -- real algorithmic overhead for a large drag on a large grid (up to 50x35 per FR-4), though bounded and only paid once per drag-release.
  evidence: confirmed by direct inspection of `_with_wall_bit` in `src/labyrinthes/domain/wall_editing.py` (rebuilds `Grid.cells` from scratch every call) and `_walls_in_zone`'s per-wall loop in `src/labyrinthes/domain/zone_editing.py`. This is an explicit, spec-sanctioned reuse tradeoff (the frozen spec's Approach: "batching `wall_editing.break_wall`/`restore_wall`... reusing those single-wall mutators... rather than duplicating the bit-twiddle") already approved by the human at CHECKPOINT 1, not an oversight -- worth revisiting only if zone drags on large grids are perceptibly slow in practice.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-3-zone-editing-destroy-restore-a-rectangular-zone.md`
  summary: `_BuilderMazeCanvas.refresh_walls()` recolors every wall item on the canvas after a zone operation (cost proportional to total grid size), not just the walls inside the dragged rectangle.
  evidence: confirmed by direct inspection of `refresh_walls()` in `src/labyrinthes/adapters/tkinter/builder/screen.py` -- pre-existing from Story 3.2's `_sync_after_wall_change()` design (unchanged by this story), now also exercised by zone operations where the mismatch between "walls touched" and "walls recolored" is more pronounced than for a single wall toggle.

## Deferred from: code review of spec-3-4-entry-and-exit-marking (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-4-entry-and-exit-marking.md`
  summary: A reopened/saved maze's previously-saved exit never re-renders on the Builder canvas because `start_builder_session` seeds `exit=None` unconditionally -- the entry renders immediately but the exit only appears once the user re-marks it.
  evidence: confirmed by direct inspection of `start_builder_session` (`src/labyrinthes/application/builder_session.py`, `exit=None` with no read of `maze.exit`) and the session-seeding I/O matrix row ("Fresh sketch session ... exit unset (nothing rendered)"). Explicitly out of scope per the spec's Design Notes ("Reopened-sketch nuance ... intentionally out of scope -- Story 3.6 owns save/load marker semantics"); flagged for Story 3.6 to close.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-4-entry-and-exit-marking.md`
  summary: Once a marker is placed there is no way to unset/clear it -- the exit can be redefined (moved) but never removed back to the "unset" state the session model already supports (`exit=None`), and the entry is always required so it has no meaningful unset.
  evidence: confirmed by direct inspection of `_place_entry`/`_place_exit` (`src/labyrinthes/adapters/tkinter/builder/screen.py`) -- neither offers any remove/clear path, and no keybinding or tool exists for it. The frozen spec's Approach/ACs never require unsetting, so this is an additive UX enhancement candidate rather than a defect; a stray misplaced exit must currently be moved somewhere rather than removed.
- source_spec: `_bmad-output/implementation-artifacts/epic-3/spec-3-4-entry-and-exit-marking.md`
  summary: The drive-by `cconfigure`→`configure` runtime-typo fix in `SettingsWindow`'s logo picker (bundled into this story's diff during implementation) has no regression test exercising the fixed navigation.
  evidence: the typo fix is in this story's diff (`src/labyrinthes/adapters/tkinter/common/settings_window.py`), but the logo-picker screen it fixes was already untested before this story (no GUI test covers `show_logo_picker`), so the corrected path is pinned by nothing. Pre-existing test gap surfaced incidentally; the fix itself was kept because it repairs a real runtime crash.
